# Stitch migration

Inspected repository ZIP: `stitch_maomao_cat_shop_storefront.zip`. Inventory: `screen.png` 854×1600, `DESIGN.md`, `code.html`. Also inspected the user-supplied homepage export at `/Users/sonny/Downloads/final_home_cat`: `screen.png` 371×1600, `DESIGN.md`, and `code.html`. Twenty-seven `lh3.googleusercontent.com` images across both exports were downloaded to `public/images/`, and production code contains no temporary asset URLs. Generated HTML was decomposed into Astro layout/components, its inline Tailwind theme became project tokens, Material Symbols became local SVG, `href="#"` became real routes, and `data-alt` became `alt`. Unsupported or unverified newsletter, social, expert, authenticity, delivery-speed, and health-outcome claims were removed or neutralized. See root `DESIGN.md` for differences and inferred responsive states.
